const plot = {
  CREATE: "Create and explain plot/world/context",
  SUMMARIZE: "Summarize adventure so far",
  REQUEST_ACTION: "Request action after analyzing previous actions/events",
};

const actions = {
  CHOOSE_POKEMON: "",
};

export default { plot, actions };
